"use client";

import { StatCard } from "@/components/stat-card";

export function StoriesCard() {
  return (
    <StatCard
      label="Stories"
      value="0 / 7"
      sub="Story bank — coming soon"
      muted
    />
  );
}
